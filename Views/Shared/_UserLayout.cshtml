<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>MovieMania - @ViewData["Title"]</title>

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />

    <!-- Google Fonts - Netflix style font -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">

    <!-- AOS Animation Library -->
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">

    <!-- Owl Carousel for sliders -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.carousel.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/assets/owl.theme.default.min.css">

    <!-- Custom Netflix-style CSS -->
    <style>
        :root {
            --netflix-red: #e50914;
            --netflix-red-hover: #f40612;
            --netflix-dark: #141414;
            --netflix-darker: #000000;
            --netflix-light: #ffffff;
            --netflix-gray: #808080;
            --netflix-light-gray: #e5e5e5;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--netflix-dark);
            color: var(--netflix-light);
            overflow-x: hidden;
        }

        /* Netflix-style scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }

        ::-webkit-scrollbar-track {
            background: var(--netflix-darker);
        }

        ::-webkit-scrollbar-thumb {
            background: var(--netflix-gray);
            border-radius: 4px;
        }

            ::-webkit-scrollbar-thumb:hover {
                background: var(--netflix-red);
            }

        /* Netflix Navbar */
        .netflix-navbar {
            background: linear-gradient(180deg, rgba(0,0,0,0.9) 0%, rgba(0,0,0,0.7) 50%, transparent 100%);
            padding: 20px 4%;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1000;
            transition: all 0.3s ease;
        }

            .netflix-navbar.scrolled {
                background: var(--netflix-darker);
                padding: 10px 4%;
            }

        .nav-logo {
            color: var(--netflix-red) !important;
            font-size: 1.8rem;
            font-weight: 700;
            text-decoration: none;
            margin-right: 40px;
            letter-spacing: 1px;
        }

        .nav-links {
            display: flex;
            gap: 20px;
        }

        .nav-link {
            color: var(--netflix-light-gray) !important;
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
            transition: color 0.3s ease;
        }

            .nav-link:hover {
                color: var(--netflix-light) !important;
            }

            .nav-link.active {
                color: var(--netflix-light) !important;
                font-weight: 600;
            }

        .nav-right {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .user-menu {
            display: flex;
            align-items: center;
            gap: 10px;
            cursor: pointer;
        }

        .user-avatar {
            width: 32px;
            height: 32px;
            background: var(--netflix-red);
            border-radius: 4px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--netflix-light);
            font-weight: 600;
        }

        .user-name {
            color: var(--netflix-light);
            font-size: 0.9rem;
        }

        .logout-btn {
            background: transparent;
            border: 1px solid var(--netflix-gray);
            color: var(--netflix-light);
            padding: 6px 15px;
            border-radius: 4px;
            font-size: 0.85rem;
            cursor: pointer;
            transition: all 0.3s ease;
        }

            .logout-btn:hover {
                border-color: var(--netflix-red);
                background: var(--netflix-red);
            }

        /* Main content */
        main {
            min-height: 100vh;
            padding-top: 80px;
        }

        /* Netflix-style dropdown */
        .dropdown-menu {
            background: rgba(0, 0, 0, 0.9);
            border: 1px solid var(--netflix-gray);
            border-radius: 4px;
            margin-top: 10px;
            padding: 8px 0;
        }

        .dropdown-item {
            color: var(--netflix-light-gray);
            padding: 8px 20px;
            font-size: 0.9rem;
            transition: all 0.2s ease;
        }

            .dropdown-item:hover {
                background: var(--netflix-gray);
                color: var(--netflix-light);
            }

            .dropdown-item i {
                margin-right: 10px;
                width: 20px;
                color: var(--netflix-red);
            }

        /* Netflix Footer */
        .netflix-footer {
            background: var(--netflix-darker);
            padding: 70px 4% 20px;
            margin-top: 50px;
            border-top: 1px solid rgba(255,255,255,0.1);
        }

        .footer-social {
            display: flex;
            gap: 25px;
            margin-bottom: 30px;
        }

            .footer-social a {
                color: var(--netflix-light-gray);
                font-size: 1.5rem;
                transition: color 0.3s ease;
            }

                .footer-social a:hover {
                    color: var(--netflix-red);
                }

        .footer-links {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            margin-bottom: 30px;
        }

        .footer-links-column h5 {
            color: var(--netflix-light-gray);
            font-size: 0.9rem;
            margin-bottom: 15px;
            font-weight: 600;
        }

        .footer-links-column ul {
            list-style: none;
            padding: 0;
        }

        .footer-links-column li {
            margin-bottom: 10px;
        }

        .footer-links-column a {
            color: var(--netflix-gray);
            text-decoration: none;
            font-size: 0.85rem;
            transition: color 0.3s ease;
        }

            .footer-links-column a:hover {
                color: var(--netflix-light);
            }

        .footer-copyright {
            text-align: center;
            color: var(--netflix-gray);
            font-size: 0.8rem;
            padding-top: 20px;
            border-top: 1px solid rgba(255,255,255,0.1);
        }

        .service-code-btn {
            background: transparent;
            border: 1px solid var(--netflix-gray);
            color: var(--netflix-gray);
            padding: 8px 20px;
            font-size: 0.85rem;
            margin-bottom: 20px;
            cursor: pointer;
            transition: all 0.3s ease;
        }

            .service-code-btn:hover {
                border-color: var(--netflix-light);
                color: var(--netflix-light);
            }

        /* Netflix-style cards and animations */
        .netflix-row {
            margin-bottom: 50px;
            padding: 0 4%;
        }

        .row-title {
            color: var(--netflix-light);
            font-size: 1.4rem;
            font-weight: 600;
            margin-bottom: 20px;
        }

        /* Loading animation */
        .netflix-loader {
            width: 100%;
            height: 100vh;
            background: var(--netflix-darker);
            display: flex;
            justify-content: center;
            align-items: center;
            position: fixed;
            top: 0;
            left: 0;
            z-index: 9999;
        }

        .loader-logo {
            color: var(--netflix-red);
            font-size: 3rem;
            font-weight: 800;
            animation: pulse 1.5s ease-in-out infinite;
        }

        @@keyframes pulse {
            0%, 100% {
                opacity: 1;
                transform: scale(1);
            }

            50% {
                opacity: 0.8;
                transform: scale(1.1);
            }
        }

        /* Responsive */
        @@media (max-width: 768px) {
            .nav-links {
                display: none;
            }

            .footer-links {
                grid-template-columns: repeat(2, 1fr);
            }

            .user-name {
                display: none;
            }
        }

        @@media (max-width: 480px) {
            .footer-links {
                grid-template-columns: 1fr;
            }
        }
    </style>

    @await RenderSectionAsync("Styles", required: false)
</head>
<body>
    <!-- Loading Animation -->
    <div class="netflix-loader" id="loader">
        <div class="loader-logo">MOVIEMANIA</div>
    </div>

    <!-- Netflix-style Navbar -->
    <nav class="netflix-navbar" id="navbar">
        <div class="container-fluid">
            <div class="d-flex align-items-center justify-content-between w-100">
                <div class="d-flex align-items-center">
                    <a href="/User/Home" class="nav-logo">
                        <i class="fas fa-film me-2"></i>MOVIEMANIA
                    </a>

                    <div class="nav-links ms-4">
                        <a href="/User/Home" class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "Index" ? "active" : "")">
                            <i class="fas fa-home me-1"></i> Home
                        </a>
                        <a href="/User/Home/Movies" class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "Movies" ? "active" : "")">
                            <i class="fas fa-film me-1"></i> Movies
                        </a>
                        <a href="/User/Home/Shows" class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "Shows" ? "active" : "")">
                            <i class="fas fa-tv me-1"></i> TV Shows
                        </a>
                        <a href="/User/Home/Anime" class="nav-link @(ViewContext.RouteData.Values["action"]?.ToString() == "Anime" ? "active" : "")">
                            <i class="fas fa-dragon me-1"></i> Anime
                        </a>
                        <a href="/User/Wishlist" class="nav-link @(ViewContext.RouteData.Values["controller"]?.ToString() == "Wishlist" ? "active" : "")">
                            <i class="fas fa-heart me-1"></i> My List
                        </a>
                    </div>
                </div>

                <div class="nav-right">
                    <div class="dropdown">
                        <div class="user-menu" data-bs-toggle="dropdown">
                            <div class="user-avatar">
                                @(User.Identity?.Name?.Substring(0, 1).ToUpper() ?? "U")
                            </div>
                            <span class="user-name">@User.Identity?.Name</span>
                            <i class="fas fa-chevron-down" style="color: var(--netflix-light-gray); font-size: 0.8rem;"></i>
                        </div>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li>
                                <a class="dropdown-item" href="/User/Profile">
                                    <i class="fas fa-user"></i> Profile
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="/User/Subscription">
                                    <i class="fas fa-crown"></i> Subscription
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="/User/Wishlist">
                                    <i class="fas fa-heart"></i> My List
                                </a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="/User/Profile/Referral">
                                    <i class="fas fa-gift"></i> Referrals
                                </a>
                            </li>
                            <li><hr class="dropdown-divider" style="border-color: var(--netflix-gray);"></li>
                            <li>
                                <form asp-controller="Auth" asp-action="Logout" method="post">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" class="dropdown-item text-danger">
                                        <i class="fas fa-sign-out-alt"></i> Logout
                                    </button>
                                </form>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        @RenderBody()
    </main>

    <!-- Netflix-style Footer -->
    <footer class="netflix-footer">
        <div class="container">
            <div class="footer-social">
                <a href="#"><i class="fab fa-facebook"></i></a>
                <a href="#"><i class="fab fa-instagram"></i></a>
                <a href="#"><i class="fab fa-twitter"></i></a>
                <a href="#"><i class="fab fa-youtube"></i></a>
            </div>

            <div class="footer-links">
                <div class="footer-links-column">
                    <h5>Quick Links</h5>
                    <ul>
                        <li><a href="/User/Home">Home</a></li>
                        <li><a href="/User/Home/Movies">Movies</a></li>
                        <li><a href="/User/Home/Shows">TV Shows</a></li>
                        <li><a href="/User/Home/Anime">Anime</a></li>
                    </ul>
                </div>

                <div class="footer-links-column">
                    <h5>My Account</h5>
                    <ul>
                        <li><a href="/User/Profile">Profile</a></li>
                        <li><a href="/User/Wishlist">My List</a></li>
                        <li><a href="/User/Subscription">Subscription</a></li>
                        <li><a href="/User/Profile/Referral">Referrals</a></li>
                    </ul>
                </div>

                <div class="footer-links-column">
                    <h5>Support</h5>
                    <ul>
                        <li><a href="#">FAQ</a></li>
                        <li><a href="#">Help Center</a></li>
                        <li><a href="#">Contact Us</a></li>
                        <li><a href="#">Privacy Policy</a></li>
                    </ul>
                </div>

                <div class="footer-links-column">
                    <h5>Legal</h5>
                    <ul>
                        <li><a href="#">Terms of Use</a></li>
                        <li><a href="#">Cookie Preferences</a></li>
                        <li><a href="#">Corporate Information</a></li>
                    </ul>
                </div>
            </div>

            <button class="service-code-btn">Service Code</button>

            <div class="footer-copyright">
                <p>&copy; @DateTime.Now.Year MovieMania. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Scripts -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>

    <!-- AOS Animation -->
    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>

    <!-- Owl Carousel -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/OwlCarousel2/2.3.4/owl.carousel.min.js"></script>

    <script>
        // Initialize AOS
        AOS.init({
            duration: 800,
            once: true
        });

        // Navbar scroll effect
        window.addEventListener('scroll', function() {
            const navbar = document.getElementById('navbar');
            if (window.scrollY > 100) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });

        // Hide loader after page load
        window.addEventListener('load', function() {
            setTimeout(function() {
                document.getElementById('loader').style.display = 'none';
            }, 1000);
        });

        // Show toast messages from TempData
        @if (TempData["Success"] != null)
        {
                <text>
                $(document).ready(function() {
                    alert('@TempData["Success"]');
                });
                </text>
        }
        @if (TempData["Error"] != null)
        {
                <text>
                $(document).ready(function() {
                    alert('@TempData["Error"]');
                });
                </text>
        }
    </script>

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>